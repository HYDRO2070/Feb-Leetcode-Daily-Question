class Solution(object):
    def maxAscendingSum(self, nums):
        sum = 0
        for i in range(len(nums)):
            if i+1 != len(nums):
                if nums[i] < nums[i+1]:
                    sum += nums[i]
                else:
                    sum = 0
        sum+=nums[-1]
        return sum
        